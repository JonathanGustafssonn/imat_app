import 'package:flutter/material.dart';

class CheckoutStep1 extends StatelessWidget {
  const CheckoutStep1({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Steg 1"),
    );
  }
}